# ============================================================================
# LineMOD 6D物体姿态估计系统 - 依赖库列表
# YOLO + GDR-Net Complete Pipeline
# ============================================================================
# 
# 安装说明：
# 1. 创建虚拟环境: python -m venv venv
# 2. 激活环境: 
#    - Windows: venv\Scripts\activate
#    - Linux/Mac: source venv/bin/activate
# 3. 安装依赖: pip install -r requirements.txt
#
# 注意事项：
# - 如果使用GPU，请先安装对应版本的PyTorch (CUDA 11.x或12.x)
# - GDR-Net推理需要CUDA支持，建议使用GPU
# - 部分可选依赖根据需求安装
# ============================================================================

# ----------------------------------------------------------------------------
# PyTorch深度学习框架 (核心依赖)
# ----------------------------------------------------------------------------
# 请根据您的CUDA版本选择安装：
# 
# CUDA 11.8:
#   pip install torch==2.0.1 torchvision==0.15.2 --index-url https://download.pytorch.org/whl/cu118
#
# CUDA 12.1:
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121
#
# CPU only:
#   pip install torch==2.0.1 torchvision==0.15.2 --index-url https://download.pytorch.org/whl/cpu
#
# 兼容GDR-Net的推荐版本 (CUDA 11.1):
torch==1.10.1
torchvision==0.11.2

# ----------------------------------------------------------------------------
# 目标检测和分割
# ----------------------------------------------------------------------------
# YOLOv8 - 用于物体检测
ultralytics>=8.0.0

# Mask R-CNN (可选，已由GDR-Net内置分割替代)
# detectron2 @ git+https://github.com/facebookresearch/detectron2.git
# 注意：detectron2安装需要编译，请参考官方文档

# ----------------------------------------------------------------------------
# 核心计算库
# ----------------------------------------------------------------------------
# NumPy - 数组计算
numpy>=1.21.0,<1.25.0  # GDR-Net兼容性限制

# OpenCV - 图像处理
opencv-python>=4.5.0,<5.0.0
opencv-contrib-python>=4.5.0,<5.0.0  # 包含额外模块

# SciPy - 科学计算
scipy>=1.7.0,<1.12.0

# ----------------------------------------------------------------------------
# 数据处理和分析
# ----------------------------------------------------------------------------
# Pandas - 数据分析和Excel处理
pandas>=1.3.0

# OpenPyXL - Excel读写支持
openpyxl>=3.0.9

# ----------------------------------------------------------------------------
# 配置和数据序列化
# ----------------------------------------------------------------------------
# PyYAML - YAML文件解析
PyYAML>=5.4.0

# ----------------------------------------------------------------------------
# 可视化
# ----------------------------------------------------------------------------
# Matplotlib - 绘图
matplotlib>=3.3.0

# Pillow - 图像库
Pillow>=8.0.0

# ----------------------------------------------------------------------------
# 3D模型处理 (可选)
# ----------------------------------------------------------------------------
# Open3D - 3D数据处理和可视化
# open3d>=0.13.0
# 注意：用于加载.ply模型文件，如果不需要3D可视化可以不安装

# Trimesh - 轻量级3D网格处理
# trimesh>=3.9.0

# PyRender - 3D渲染 (GDR-Net可选)
# pyrender>=0.1.45

# ----------------------------------------------------------------------------
# 工具库
# ----------------------------------------------------------------------------
# tqdm - 进度条
tqdm>=4.60.0

# packaging - 版本比较
packaging>=21.0

# pathlib2 - 路径处理兼容 (Python 3.4+已内置pathlib)
# pathlib2>=2.3.0  # 仅Python < 3.4需要

# ----------------------------------------------------------------------------
# GDR-Net特定依赖
# ----------------------------------------------------------------------------
# MMCV - GDR-Net使用的计算机视觉库
# 注意：如果使用GDR-Net官方实现，需要安装MMCV
# CUDA 11.1版本:
#   pip install mmcv-full==1.3.9 -f https://download.openmmlab.com/mmcv/dist/cu111/torch1.10/index.html
# 其他版本请参考: https://mmcv.readthedocs.io/en/latest/get_started/installation.html
#
# 本项目已使用纯PyTorch实现，不强制需要MMCV

# EasyDict - 配置字典 (GDR-Net使用)
easydict>=1.9

# ----------------------------------------------------------------------------
# 开发和测试 (可选)
# ----------------------------------------------------------------------------
# Pytest - 单元测试
# pytest>=7.0.0
# pytest-cov>=3.0.0

# Black - 代码格式化
# black>=22.0.0

# Flake8 - 代码检查
# flake8>=4.0.0

# MyPy - 类型检查
# mypy>=0.950

# ----------------------------------------------------------------------------
# 其他依赖
# ----------------------------------------------------------------------------
# Scikit-learn - 机器学习工具
scikit-learn>=0.24.0

# ----------------------------------------------------------------------------
# 版本兼容性说明
# ----------------------------------------------------------------------------
# Python版本要求: Python >= 3.8, < 3.11 (推荐 3.8 或 3.9)
# CUDA要求: CUDA >= 11.1 (GPU模式)
# 
# 已测试环境：
# - Python 3.8 + PyTorch 1.10.1 + CUDA 11.1 ✓
# - Python 3.9 + PyTorch 2.0.1 + CUDA 11.8 ✓
# - Python 3.10 + PyTorch 2.1.0 + CUDA 12.1 ✓
# 
# 注意：GDR-Net官方权重和配置基于PyTorch 1.10.1训练，
# 使用更高版本PyTorch可能需要微调配置。
# ============================================================================
